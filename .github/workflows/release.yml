# name: Release & Notify

# on:
#   push:
#     branches:
#       - production

# permissions:
#   contents: write
#   issues: write

# jobs:
#   auto-release:
#     name: Semantic Release
#     runs-on: ubuntu-latest

#     steps:
#       - name: Checkout
#         uses: actions/checkout@v3

#       - name: Use Node.js
#         uses: actions/setup-node@v3
#         with:
#           node-version: "20"
#           registry-url: "https://registry.npmjs.org/"

#       - name: Install dependencies
#         run: npm install

#       - name: Run semantic-release
#         env:
#           GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
#         run: npx semantic-release

#   send-success-email:
#     name: Send Email on Success
#     needs: auto-release
#     runs-on: ubuntu-latest
#     if: ${{ needs.auto-release.result == 'success' }}

#     steps:
#       - name: Send Success Email
#         uses: dawidd6/action-send-mail@v3
#         with:
#           server_address: smtp.gmail.com
#           server_port: 465
#           secure: true
#           username: ${{ secrets.EMAIL_USERNAME }}
#           password: ${{ secrets.EMAIL_PASSWORD }}
#           subject: 🚀 New Release Published on Production
#           to: premkumardanav111@gmail.com
#           from: MEC Release Bot <${{ secrets.EMAIL_USERNAME }}>
#           content_type: text/html
#           html_body: |
#             <h2>🎉 New Release Published</h2>
#             <p><strong>📌 Branch:</strong> production</p>
#             <p><strong>📋 Info:</strong> Semantic-release successfully created a new version.</p>
#             <p><strong>🔗 View it here:</strong>
#               <a href="https://github.com/${{ github.repository }}/releases" target="_blank">
#                 GitHub Releases
#               </a>
#             </p>

#   send-failure-email:
#     name: Send Email on Failure
#     needs: auto-release
#     runs-on: ubuntu-latest
#     if: ${{ needs.auto-release.result == 'failure' }}

#     steps:
#       - name: Send Failure Email
#         uses: dawidd6/action-send-mail@v3
#         with:
#           server_address: smtp.gmail.com
#           server_port: 465
#           secure: true
#           username: ${{ secrets.EMAIL_USERNAME }}
#           password: ${{ secrets.EMAIL_PASSWORD }}
#           subject: ❌ Release Failed on Production
#           to: premkumardanav111@gmail.com
#           from: MEC Release Bot <${{ secrets.EMAIL_USERNAME }}>
#           content_type: text/html
#           html_body: |
#             <h2>❌ Release Failed</h2>
#             <p><strong>📌 Branch:</strong> production</p>
#             <p><strong>⚠️ Info:</strong> Semantic-release encountered an error and failed to publish the release.</p>
#             <p><strong>🛠️ Action Required:</strong> Check the GitHub Actions logs for details.</p>
